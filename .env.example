# On-demand ISR Revalidation Secret
# Generate a random secret for production (e.g., using openssl rand -base64 32)
# This is used to authenticate requests to /api/revalidate
REVALIDATE_SECRET=your_random_secret_here
